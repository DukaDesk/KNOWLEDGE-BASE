   # Published app delivery - backend TODO

Date: 2026-09-20
Status: Open - backend implementation required
Repository: D:/work/DD/DUKA-BACKEND
Owner: Backend agent (infrastructure support for storage/deployment)
Read: [Evidence and coordinated plan](../ARCHITECTURE/PUBLISHED_APP_DELIVERY_FIX_PLAN_2026-09-20.md). These tasks supersede the broad publication items in the older logo incident; retain its evidence.

- [ ] **B1 / P0 - Validate and preserve submitted PublishedApp.** In publishing.service.ts replace the array-only branch with a DTO/schema validator for the canonical 1.0.0 object-screen manifest. If a manifest is supplied but invalid, return 400/422 with field errors; never fall through to draft compilation. Validate tenant ownership, schema, required sections, screen/action references and manifest/request version consistency. Preserve all submitted fields. Test with the exact KB shared fixture and a second changed template; assert compiler is not invoked.
- [ ] **B2 / P0 - One atomic release/activation operation.** Make ManifestCompiler compile without creating a competing Release. Align draft compilation to 1.0.0, or explicitly reject that legacy publish mode until supported. Publish in a transaction with safe version/build allocation, immutable records, idempotent retries and active production reference. Remove unconditional draft deletion; retain editable drafts or apply a documented revision policy. Test unique conflicts, concurrent requests, failures and retries; only one release becomes active and no successful response precedes durable activation.
- [ ] **B3 / P0 - One canonical public reader.** Share release resolution between RendererService and MobileBffService. Read the active production release, preserve its full snapshot and expose release identity/checksum via a consistent receipt/headers. Remove owner-page and nested-config fallback from the production path. Use typed no-published-release and unsupported-contract errors. Filter channel/publication/visibility correctly; explicit version reads must not expose private drafts. Test both routes by tenant ID/slug, absent release, withdrawn tenant and archived/draft versions.
- [ ] **B4 / P0 - Migration and recovery.** Add/backfill the active-release reference from validated existing published releases, audit tenant status/channel/version conflicts and isolate malformed legacy records. Recover the intended latest merchant project from authenticated history or republish it; do not guess from config.deployed. Record the deployed backend commit and correlate the user's publish response with stored release rows. Define rollback of the additive migration.
- [ ] **B5 / P0 - Cache, rollback and authorization.** Make activation/cache invalidation coherent for ID, current/previous slugs and rollback; protect against a stale reader repopulating the cache after activation. Prefer immutable release caches plus fresh active-reference resolution. Use transactional outbox/retry for post-commit events/cache repair rather than reporting an ambiguous failed publication after committing. Enforce owner/manager membership on rollback and tenant-scoped publishing history/draft endpoints (current rollback controller supplies no user ID). Test warmed caches, races, Redis outage, aliases and cross-tenant callers.
- [ ] **B6 / P0 - Fix media delivery at source.** In media.service.ts retain URLs returned by StorageService for original and optimized uploads. Use distinct keys or skip deletion when original and optimized keys match; add a .webp upload regression proving the final object remains. Do not advertise an original URL after deleting that file. Route presets through the configured durable storage provider too. Verify anonymous GET of every returned URL, S3/CDN paths, content type, and durability across restart/deploy. Check serving paths against upload directories. Follow the existing Builder/Media TODO for wider upload-contract requirements.
- [ ] **B7 / P1 - Discovery and runtime compatibility.** Project published display name/slug/logo and active release identity into discovery only after activation; reconcile tenant.status/publishedAt. Validate supported schema/runtime/component/action capabilities and asset references before activation. Publish a machine-readable compatibility contract used by merchant preflight and mobile. Return a clear incompatible-runtime outcome; never activate an app that the target shell cannot render.
- [ ] **B8 / Release gate - Integration evidence.** Run the plan's A-to-B-to-A scenario using real authenticated publish and rollback APIs, both anonymous read paths and durable media. Attach request IDs, versions/checksums, tests, migration result and deployed commit to the plan. Success is not established by a POST 200 or a saved config object alone.

Backend fixes belong in this repository. Mobile must not compensate for dropped manifests, deleted WebP files or stale server caches. Mark tasks complete only with implementation and test evidence.

## Client integration handoff (2026-09-20)

The mobile/merchant consumers are implemented locally; see the [client report](../mobile/PUBLISHED_APP_DELIVERY_CLIENT_IMPLEMENTATION_2026-09-20.md). Merchant sends Idempotency-Key and expects releaseId/version/checksum on publish, or recovers those from GET publishing/releases/{version} containing the exact manifest. It verifies definition plus BFF by tenant ID before success and uses POST publishing/rollback/{version}. Ensure these routes share the active production release. The client does not retry uncertain mutations automatically and does not repair backend config. Mobile retains a visibly warned legacy read until B1-B5 are deployed. All backend tasks above remain open.

Splash/tab contract: persist and serve the complete submitted splash (enabled, durationMs, screen) and navigation (root, tabs, style) unchanged. Mobile alone runs the tenant splash lifecycle; backend must not inject another timer/startup screen. Current live v0.0.7 has root tabs but no splash. See [client evidence](../mobile/SPLASH_AND_MANIFEST_TABS_2026-09-20.md).

## 2026-09-20: Builder press attachment acceptance checks

- [ ] Preserve component props.tapAction and individual cats/categories/items/offers/orders/rows[].tapAction ActionDef objects through publish, persistence and both canonical read paths.
- [ ] Preserve merged node.actions, including null entries that clear an event, without replacing them with template defaults. Backend delivers the selected snapshot; mobile executes its actions.
- [ ] Add delivery round-trip coverage with two category pills/menu cards targeting different screen IDs. See ../builder/APP_SECTIONS_AND_PRESS_ACTIONS_2026-09-20.md. No new endpoint is required.
